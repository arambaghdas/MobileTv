package train.apitrainclient.listeners;

import android.view.View;

public interface OnLayoutPopulatedListener {
    //litener for manipulating the table on the graph
    public void onLayoutPopulated(View view, View view2, View view3);
}
